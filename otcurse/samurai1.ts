// const name = 'Felipe'
// const age = 25

// '2' + 2
// // resultado: 22

const fullName = (user) => {
    return `${user.firstName} ${user.lastName}`
}

const user = {
    firstName: 'Enzo',
    lastName: 'Luiz'
}

fullName(user)